<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  ExportOptions.plist — used by Codemagic to export an UNSIGNED IPA
  suitable for AltStore / sideloading.
  
  'method' is set to 'ad-hoc' so no Apple Developer account is required.
  AltStore will re-sign the IPA with the user's personal Apple ID (free cert).
-->
<plist version="1.0">
<dict>
    <key>method</key>
    <string>ad-hoc</string>

    <key>compileBitcode</key>
    <false/>

    <key>stripSwiftSymbols</key>
    <true/>

    <key>thinning</key>
    <string>&lt;none&gt;</string>

    <!-- No team ID needed — AltStore signs with user's personal Apple ID -->
    <key>signingStyle</key>
    <string>manual</string>

    <key>signingCertificate</key>
    <string></string>

    <key>provisioningProfiles</key>
    <dict/>
</dict>
</plist>
